const { SlashCommandBuilder } = require('discord.js');

module.exports = {
    data : new SlashCommandBuilder()
	.setName('botinfo')
	.setDescription('Get info about a user or a server!')
	.addSubcommand(subcommand =>
		subcommand
			.setName('credits')
			.setDescription('Mengenal lebih dekat developer bot ini')	
	.addSubcommand(subcommand =>
		subcommand
			.setName('jumlah_server')
			.setDescription('Menunjukkan jumlah server yang menggunakan bot ini'))
	.addSubcommand(subcommand =>
		subcommand
			.setName('jumlah_user')
			.setDescription('Menunjukkan jumlah user yang menggunakan bot ini'))
	),

	async execute(interaction, client) {
		const subcommand = interaction.options.getSubcommand();
		if (subcommand === 'credits') {

		}
		else if (subcommand === 'jumlah_server') {

		}
		else if (subcommand === 'jumlah_user') {

		}
		
	}
}